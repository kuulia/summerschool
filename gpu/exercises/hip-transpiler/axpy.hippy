const int N = 1024
const int n = 256

kernel axpy(a: float, x: float*, y: float*, n: int):
    i = global_index()
    if i < n:
        y[i] += a * x[i]

init a = 3.0, x = linspace(0.0, 1.0), y = linspace(0.0, 100.0)
buffer float a, x(N), y(N)
upload x, y
launch axpy(N)(a, x, y, n)
download y
print x, y
